# joda — production dependencies (fully pinned)
# Install into a venv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- Separation engine ---
demucs==4.1.0
numpy==2.5.1              # required by demucs at import time; pin explicitly

# --- Web / API ---
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.51.0
python-multipart==0.0.32  # multipart/form-data uploads
pydantic-settings==2.7.1  # env-driven config

# --- Async job queue ---
rq==2.4.1                 # Redis-backed task queue
redis==5.2.1

# --- Input validation ---
filetype==1.2.0           # magic-byte content sniffing (not just extension)
