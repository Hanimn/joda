# Track Separation POC — Path B (local Demucs)
# Install into a venv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
demucs==4.1.0
numpy            # required by demucs at import time; not always pulled transitively
fastapi
uvicorn[standard]
python-multipart # required by FastAPI for multipart/form-data file uploads
